server:
  port: 8089
spring:
  application:
    name: apigateway-service
  cloud:
    config:
      enabled: true
      #uri: http://localhost:9972
      uri: http://localhost:8888
  security:
    user:
      name: root
      password: root